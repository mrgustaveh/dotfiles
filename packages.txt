# Core desktop
hyprland
waybar
kitty
rofi
stow

# Apps opened from Hyprland keybinds / scripts
btop
# yazi: installed from GitHub releases by install.sh (not in apt)
# google-chrome-stable: installed via Google apt repo by install.sh

# Hyprland utilities
grim
slurp
wl-clipboard
brightnessctl
wireplumber
hypridle
hyprlock
# swww and swaync are not in Ubuntu/Pop apt; install manually if needed
socat
jq

# Fonts used by waybar/kitty config
fonts-jetbrains-mono
